Рекурсія: час O(2ⁿ), бо кожен виклик породжує два підвиклики, пам’ять O(n) через стек рекурсії.

Динамічне програмування (DP): час O(n), бо кожне значення обчислюється один раз, пам’ять O(n) через масив memo.

Ітерація: час O(n), бо один цикл, пам’ять O(1), бо використовуються тільки три змінні.
